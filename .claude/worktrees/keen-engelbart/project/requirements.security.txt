# Optional add-ons (camera/security)
#
# This pulls in torch via ultralytics, which can be very large. Install only if
# you explicitly want the camera/security suite.
#
# Usage:
#   pip install -r requirements.txt
#   pip install -r requirements.security.txt

ultralytics>=8.0.0        # YOLOv8 (includes torch dependency)
opencv-python-headless>=4.9.0  # Use headless on servers, opencv-python on desktop
pyngrok>=7.0.0            # Ngrok Python wrapper

