# ══════════════════════════════════════════════════════════════
#  Macro Intelligence trading system
#  requirements.txt
# ══════════════════════════════════════════════════════════════
#
#  Full install:  pip install -r requirements.txt
#  Install:  pip install -r requirements.txt
#
#  Tested on Python 3.10 / 3.11 / 3.12
# ══════════════════════════════════════════════════════════════

# ── Web Framework ─────────────────────────────────────────────
flask>=3.0.0
flask-socketio>=5.3.6
flask-cors>=4.0.0
gevent>=23.9.1
gevent-websocket>=0.10.1

# ── Data & Math ───────────────────────────────────────────────
numpy>=1.26.0
pandas>=2.1.0
scipy>=1.11.0

# ── Machine Learning ──────────────────────────────────────────
xgboost>=2.0.0
scikit-learn>=1.3.0
shap>=0.44.0
lightgbm>=4.0.0
catboost>=1.2.0

# ── Finance Data ──────────────────────────────────────────────
yfinance>=0.2.36          # Free price data fallback (no key needed)
requests>=2.31.0          # Alpha Vantage, Polygon, EODHD, Binance, Bybit HTTP calls

# ── Real-Time WebSocket ───────────────────────────────────────
websocket-client>=1.7.0   # Finnhub WebSocket

# ── Sentiment Analysis ────────────────────────────────────────
nltk>=3.8.1               # VADER sentiment (fast, rule-based, free)

# ── Database ──────────────────────────────────────────────────
# SQLite is built into Python — no extra package needed

# Legacy operator extras were removed from the public trading build.

# ── Utilities ─────────────────────────────────────────────────
python-dotenv>=1.0.0      # Load .env file automatically
MetaTrader5>=5.0.45; python_version < "3.13" and platform_system == "Windows"   # Optional MT5 bridge (best on Python 3.11/3.12)

# ══════════════════════════════════════════════════════════════
# OPTIONAL — only if you want the full feature set
# ══════════════════════════════════════════════════════════════

# Alpha Vantage official client (we use requests directly, but optional)
# alpha_vantage>=2.3.1

# Upstox SDK for live NSE/BSE data
# upstox-python-sdk>=2.0.0

# Plotly for richer charts in the dashboard
# plotly>=5.18.0

# Redis for production signal caching (SQLite works fine for dev)
# redis>=5.0.0
